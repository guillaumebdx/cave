{% extends 'base.html.twig' %}

{% block title %}Hello WineController!{% endblock %}

{% block body %}
    <div class="create-wine">
        <h1>Créer un vin</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button>OK</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
